<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <% @songs.each do |song|%>
      <p><a href= "/songs/<%= song.slug %>"> <%= song.name %> </a></p>
    <% end %>
  </body>
</html>
